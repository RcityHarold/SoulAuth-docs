# 中文文档写作约定

英文站是先写的，中文站曾经是**逐句译过来**的 —— 结果是英文的信息结构被原样保留，
读起来像机翻。这份约定是为了让中文页面按中文写，而不是按英文的顺序说中文。

其中可机器检查的部分由 `scripts/check-zh-style.mjs` 守着。

## 一 · 语域

**正式、克制、技术文档体。** 面向的是准备把这套东西部署到生产的工程师。

不要口语：`弄个身份`、`丢进一个组`、`这套一直能用`、`搞定`。
也不要公文腔：`本系统旨在`、`予以`、`加以`。

判据：这句话出现在一份对外发布的技术手册里，会不会显得随便或做作。

## 二 · 术语

同一个概念只用一个名字。

| 用 | 不用 |
|---|---|
| AI 主体 | AI Agent、Agent、智能体、代理 |
| 非人主体 | 仅在与「人类主体」对举时使用 |
| 主体 | 泛指任何 Actor 时使用 |
| 会话 | session（正文中） |
| 凭证 | credential（正文中） |
| 令牌 | token（正文中） |

代码标识符保持原样并加反引号：`ActorIdentity`、`HumanAccount`、`subject_key`。
它们是标识符，不是术语，不翻译。

首次出现的关键术语可在括号内附英文：AI 主体（AI actor）。同一页不重复附注。

## 三 · 标点

**破折号 `——` 是英文 em-dash 的直译，克制使用。** 中文里它表示解释或转折，
一页出现三次以上通常说明句子该拆开了。多数情况下应改为：

- 冒号（引出解释）
- 分号（并列的两个完整意思）
- 句号（本来就是两句话）

**引号统一用「」。** 不混用 `""`。代码、配置项、字段名用反引号，不用引号。

中英文之间加空格：`APP_URL 是公开地址`，不写 `APP_URL是公开地址`。

## 四 · 句式

**避免用「它」开头。** 英文靠代词维持指代链，中文靠省略主语或复指名词。

> ✗ 它不校验凭证。它只回答这个主体是谁。
> ✓ 身份域不校验凭证，只回答「这个主体是谁」。

**避免「被……的」作定语。** 改主动或改述。

> ✗ 这一点是**被断言的**而不是被承诺的。
> ✓ 这一点由测试断言，不是靠承诺。

**长定语拆开。** 中文的修饰语前置能力弱于英文。

> ✗ 一个能改写自己表结构的认证服务
> ✓ 一个认证服务如果能改写自己的表结构

**结论与铺垫的顺序按中文来。** 英文习惯先给结论再解释；中文技术写作常常先给
事实再收结论。逐句翻译会把英文的顺序也搬过来，读着突兀。

## 五 · 与英文页的关系

中文页**不是**英文页的译文，两者是同一事实的两种表达。

- 事实必须一致：数字、端点、配置项、状态徽章、代码示例逐字相同。
- 表达可以不同：段落切分、比喻、举例的详略都可以按语言习惯调整。
- 结构必须一致：标题层级与顺序对齐，`check:figures` 与 `check:status`
  依赖这一点。
