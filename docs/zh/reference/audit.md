# 审计

## 记录的内容

认证事件——登录成功与失败、OAuth 登录、登出、口令重置、MFA 失败、权限拒绝、
限流违规、账号上锁与解锁。

写入方遵守两条规则：

- **绝不阻塞请求。** 写入是 fire-and-forget；失败只打日志，用户的操作照常完成。
  代价是事件可能整个丢掉 —— 写入跑在 spawn 出去的任务里，进程在它完成之前退出，
  这条事件就没了，连日志都不会有。加上这张表本身不防篡改，请把它当作运维记录，
  不是证据。
- **绝不记录凭据。** 只记 action、分类、状态、IP、User-Agent 和少量非敏感上下文。

## 审计端点

<!-- table-only: /api/audit/** — 按时间窗口取的只读报表，查询参数形状一致，彼此独立。 -->
<ApiTable tag="Audit" />

看板与报表查询接受时间窗口参数。服务端会对该参数做上限截断，因此一个过大的 `days`
值得到的是一个有界窗口，而不会触发全表扫描。

## 接下来

| | |
|---|---|
| 谁能读审计数据 | [管理](/zh/reference/administration) |
| 安全模型覆盖与不覆盖什么 | [威胁模型](/zh/security/threat-model) |
